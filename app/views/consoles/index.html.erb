<p id="notice"><%= notice %></p>

<h1>Consoles</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @consoles.each do |console| %>
      <tr>
        <td><%= console.title %></td>
        <td><%= console.description %></td>
        <td><%= console.image %></td>
        <td><%= link_to 'Show', console, :class => "button", :role => "button" %></td>
        <td><%= link_to 'Edit', edit_console_path(console), :class => "button", :role => "button" %></td>
        <td><%= link_to 'Destroy', console, :class => "button", :role => "button", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Console', new_console_path, :class => "button", :role => "button" %>
